{
  "name": "caobiji-review-data",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "validate": "node scripts/generate-review.mjs --validate-only",
    "generate": "node scripts/generate-review.mjs --force",
    "generate:scheduled": "node scripts/generate-review.mjs --scheduled",
    "generate:dry": "node scripts/generate-review.mjs --dry-run"
  },
  "dependencies": {
    "playwright": "1.52.0"
  },
  "engines": {
    "node": ">=20"
  }
}
